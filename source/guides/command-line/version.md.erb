---
title: "Рукодство - Командная строка: Версия"
---

# Командная строка

## Версия

Запуск команды `hanami version` выведет текущую версию используемого фреймворка.

```shell
% bundle exec hanami version
v<%= hanami_version %>
```

У нее есть так же сокращения `--version` и `-v`.

```shell
% bundle exec hanami --version
v<%= hanami_version %>

% bundle exec hanami -v
v<%= hanami_version %>
```